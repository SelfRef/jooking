type State = {
	name: string;
	description: string;
};

export const state = (): State => ({
	name: 'Hotelix',
	description: 'Jakies info o stronie',
});

export const mutations = {};

export const getters = {};
